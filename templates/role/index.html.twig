{% extends 'base.html.twig' %}

{% block title %}{{ 'app.title.role.index' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid">
        <div id="content" class="container-fluid" {{ stimulus_controller('table', 
                    { 'url': path('role_index'),
                    'exportName': 'roles',
                })}} data-action="entity:success->table#refreshContent" data-table-target="content">
                {{ include('role/_list.html.twig', { 
                    'roles': roles,
                    'pageSize' : pageSize,
                    'page': page,
                    'sortName': sortName,
                    'sortOrder': sortOrder,
                }) }}
        </div>
	</main>
{% endblock %}