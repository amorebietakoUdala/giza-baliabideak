{% extends 'base.html.twig' %}

{% block title %}{{ 'app.title.application.index' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid">
        <div id="content" class="container-fluid" {{ stimulus_controller('table', 
                    { 'url': path('application_index'),
                    'exportName': 'applications',
                })}} data-action="entity:success->table#refreshContent" data-table-target="content">
                {{ include('application/_list.html.twig', { 
                    'applications': applications,
                    'pageSize' : pageSize,
                    'page': page,
                    'sortName': sortName,
                    'sortOrder': sortOrder,
                }) }}
        </div>
	</main>
{% endblock %}